name: cellcounter
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  # - bioconda::fiji
  # GPU acceleration (only CuPy from conda works)
  # - cuda-toolkit
  - pip:
      # - cellcounter[gpu]
      - -e ".[gpu]"
